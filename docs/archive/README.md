# Docs Archive

This folder stores historical plan snapshots and retired working documents.

Rules:

- archive material here when it is still worth keeping but should not remain in the main `docs/` entry surface
- do not treat archive files as the current source of truth
- if a useful enduring lesson exists in an archived file, promote that lesson into `docs/wiki/`

Current contents:

- `plans/2026-04-13/` for the April 13 ultra-plan snapshots
